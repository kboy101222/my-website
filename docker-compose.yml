services:
  django:
    container_name: django_server
    build: .
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/usr/src/app/my-website
    ports:
      - "8000:8000"
    restart: unless-stopped